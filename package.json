{
  "name": "neo-website",
  "version": "1.0.0",
  "type": "module",
  "description": "Neo Solutions Landing Page",
  "main": "index.html",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "check": "cmp index.html neo-hero.html && for f in js/*.js; do node --check \"$f\"; done && node --check api/lead.js && echo 'OK: entrypoints in sync, JS valid'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chatgptricks/neosolutions.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
